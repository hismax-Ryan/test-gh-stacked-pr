import GreetingCard from "./components/GreetingCard";

export default function App() {
  return (
    <main className="app">
      <h1>Stack PR Demo</h1>
      456
      <p>A tiny React + TypeScript app for trying stacked pull requests.</p>
      <GreetingCard />
    </main>
  );
}
